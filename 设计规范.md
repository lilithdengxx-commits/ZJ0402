# 网络舆情系统 · 设计规范

> 本规范从现有页面中提炼，用于指导后续所有页面的设计，确保产品风格统一。

---

## 一、设计原则

- **扁平克制**：无毛玻璃、无重阴影、无大圆角；用细描边与极淡背景色区分层级
- **信息密度优先**：压缩间距，同等空间展示更多有效信息
- **主色调一致**：全站以蓝色 `#2b61f0` 为主色，点缀绿/橙/红三个语义色
- **可读性第一**：正文不小于 12px，标题层级清晰，行高 1.6

---

## 二、色彩系统

### CSS 变量定义（`:root`）

```css
--text:         #1e2d45   /* 主文本，深蓝灰 */
--text-soft:    #6b7c95   /* 次要文本 */
--text-faint:   #9eb0c6   /* 弱文本、时间戳、占位 */
--primary:      #2b61f0   /* 主色：按钮、链接、激活态、数值 */
--primary-deep: #1a4ed4   /* 主色按下/深色变体 */
--secondary:    #7ab0ff   /* 辅助蓝 */
--secondary-soft: rgba(43,97,240,0.10)  /* 主色极淡背景 */
--success:      #12a87a   /* 正面/完成/好 */
--danger:       #e04040   /* 风险/删除/负面 */
--warning:      #c47d10   /* 待核实/警告/橙 */
--sidebar-bg:   #ffffff
--sidebar-width: 220px
```

### 页面背景
```
body background: #eef1f8
```

### 常用颜色速查

| 用途 | 值 |
|---|---|
| 卡片背景 | `#ffffff` |
| 卡片内嵌子块背景 | `rgba(43,97,240,0.03)` |
| 卡片边框 | `1px solid rgba(122,145,184,0.10)` |
| 子块边框 | `1px solid rgba(122,145,184,0.12)` |
| 内容分割线 | `1px solid rgba(122,145,184,0.08)` |
| 顶栏底部线 | `1px solid rgba(122,145,184,0.14)` |
| 渐变 header | `linear-gradient(135deg, #2b61ea 0%, #4e88ff 56%, #77b6ff 100%)` |

---

## 三、字体

```css
font-family: "PingFang SC", "Lantinghei SC", "Source Han Sans SC",
             "Noto Sans SC", "Microsoft YaHei UI", "微软雅黑", sans-serif;
```

### 字号层级

| 角色 | 大小 | 字重 | 颜色 |
|---|---|---|---|
| 大标题（报告/页面主标题） | 22px | 900 | `#fff`（在深色 header 上）|
| 卡片标题 / 区块标题 | 15px | 800 | `#1a2234` |
| 列表项标题 / 摘要加粗 | 13px | 700–800 | `#1a2234` |
| 正文 / 摘要 | 12px | 400 | `#68778f` |
| 辅助信息 / 时间戳 / 标签 | 11px | 600–700 | `#9aa8bc` |
| 数值大字（统计卡片） | 26px | 800 | `var(--primary)` |
| 数值中字（效果卡片） | 24px | 800 | 语义色 |
| 顶栏面包屑 | 13px | 600 | `var(--text-soft)` |
| 导航项 | 14px | 400（默认）/ 700（激活） | `#4a5c7a` / `var(--primary)` |
| 导航分组标题 | 11px | 700 | `#8a9ab8`（大写，letter-spacing: 0.06em）|

---

## 四、布局

### 整体骨架

```
┌──────────────────────────────────────────────┐
│  Sidebar (220px, sticky, 白色, 无圆角)        │
│  ┌────────────────────────────────────────┐  │
│  │  Topbar (44px, sticky, 白色)           │  │
│  ├────────────────────────────────────────┤  │
│  │  Page Content (背景 #eef1f8)           │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

```css
.app-shell {
  display: grid;
  grid-template-columns: var(--sidebar-width) 1fr;
  min-height: 100vh;
}
```

### 页面内容区

```css
.page {
  padding: 14px 16px 32px;
  display: flex;
  flex-direction: column;
  gap: 20px;   /* 卡片之间的间距 */
}
```

---

## 五、组件规范

### 5.1 侧边栏 Sidebar

```
宽度:   220px
背景:   #ffffff
阴影:   box-shadow: 2px 0 8px rgba(26,44,82,0.08)
圆角:   0（贴边）
定位:   sticky top:0, height:100vh
```

**品牌区 Brand**
```
高度:   44px
图标:   26×26px SVG（蓝色渐变三棱柱）
标题:   14px, 700, 同 --text
底部线: 1px solid rgba(0,0,0,0.07)
```

**导航分组标题**
```
字号:   11px, 700, 全大写, letter-spacing:0.06em
颜色:   #8a9ab8
内边距: 20px 6px 5px（上留白大，形成区块感）
```

**导航项**
```
高度:   min-height 34px
内边距: 5px 10px 5px 14px
圆角:   7px
默认色: #4a5c7a, 14px, 400
悬停:   background rgba(43,97,240,0.06)
激活:   background rgba(43,97,240,0.09), color var(--primary), 700
激活条: 左侧 3px 竖条, var(--primary), border-radius:999px
```

---

### 5.2 顶栏 Topbar

```
高度:   44px, sticky, z-index:50
背景:   #ffffff
底部线: 1px solid rgba(122,145,184,0.14)
阴影:   0 1px 4px rgba(26,44,82,0.06)
内边距: 0 16px 0 14px
```

**面包屑结构**
```
父级:  13px, 500, var(--text-faint)
分隔: "/" 符号, #c5cdd9, 12px
当前: 13px, 600, var(--text-soft)
```

**右侧工具栏**
- 图标按钮：30×30px，圆角 7px，悬停 `rgba(43,97,240,0.08)`
- 头像胶囊：`border-radius:999px`，`background:rgba(43,97,240,0.07)`，内含 24×24 蓝色圆形徽章

---

### 5.3 标准白色卡片

```css
background: #ffffff;
border-radius: 10px;
border: 1px solid rgba(122,145,184,0.10);
box-shadow: 0 1px 4px rgba(26,44,82,0.04);
padding: 16px 20px;   /* 常规 */
/* 大卡片: padding 18px 22px */
```

> **⚠ 圆角统一为 10px，禁止使用 16px / 20px**

---

### 5.4 卡片内嵌子块（stat / effect card）

```css
background: rgba(43,97,240,0.03);
border-radius: 8px;
border: 1px solid rgba(122,145,184,0.12);
padding: 12px 16px;
```

---

### 5.5 筛选栏 + 列表 合并卡片

筛选栏和列表共享一个卡片轮廓，筛选栏为上半部分，列表为下半部分：

```css
/* 筛选栏（上） */
.filter-bar {
  border-radius: 10px 10px 0 0;
  border: 1px solid rgba(122,145,184,0.10);
  border-bottom: none;
  padding: 14px 18px;
  background: #fff;
}

/* 列表卡片（下） */
.list-card {
  border-radius: 0 0 10px 10px;
  border: 1px solid rgba(122,145,184,0.10);
  box-shadow: 0 1px 4px rgba(26,44,82,0.04);
}
```

---

### 5.6 渐变 Header 卡片

用于报告/详情页顶部突出展示：

```css
background: linear-gradient(135deg, #2b61ea 0%, #4e88ff 56%, #77b6ff 100%);
border-radius: 10px;
padding: 22px 28px;
color: #fff;
```

内部文字层次：
- 主标题：22px, 900
- 副标题/概述：12px, `rgba(255,255,255,0.85)`, 行高 1.7, margin: 10px 0 14px
- 元信息（日期/地域）：12px, `rgba(255,255,255,0.85)`
- 元信息强调值：14px, `#fff`, 700

---

### 5.7 区块标题（带竖条）

```css
.section-label {
  font-size: 13px;
  font-weight: 800;
  color: #1a2234;
  display: flex;
  align-items: center;
  gap: 8px;
}
.section-label::before {
  content: '';
  width: 4px;
  height: 14px;
  border-radius: 2px;
  background: var(--primary);
  display: block;
}
```

---

### 5.8 按钮

#### 主按钮（Primary）
```css
background: var(--primary);    /* #2b61f0 */
color: #fff;
border: none;
border-radius: 8px;
padding: 7px 16px;
font-size: 13px;
font-weight: 700;
box-shadow: 0 4px 12px rgba(43,97,240,0.25);
```

#### 次要按钮（Secondary）
```css
background: #fff;
color: #68778f;
border: 1px solid rgba(122,145,184,0.20);
border-radius: 8px;
padding: 7px 16px;
font-size: 13px;
font-weight: 700;
```

#### 幽灵按钮（Ghost，在深色背景上）
```css
background: rgba(255,255,255,0.18);
color: #fff;
border: 1px solid rgba(255,255,255,0.35);
border-radius: 8px;
```

> **⚠ 按钮圆角统一为 8px，禁止使用 12px / 20px**

---

### 5.9 输入控件（Select / Input）

```css
padding: 6px 28px 6px 10px;   /* select 右侧留 arrow 空间 */
border-radius: 8px;
border: 1px solid rgba(122,145,184,0.15);
background: #f8fbff;
color: #2a3448;
font-size: 13px;
```

---

### 5.10 状态标签（Pill）

```css
display: inline-flex;
align-items: center;
gap: 4px;
padding: 3px 10px;        /* 小标签 */
/* 或 padding: 4px 12px; 普通标签 */
border-radius: 999px;
font-size: 11px;
font-weight: 700;
```

| 状态 | 背景 | 文字 |
|---|---|---|
| 成功/正面/完成 | `rgba(25,165,129,0.12)` | `var(--success)` |
| 进行中 | `rgba(250,173,20,0.12)` | `#c87611` |
| 蓝色/正常 | `rgba(47,107,255,0.10)` | `var(--primary)` |
| 警告/待核实 | `rgba(186,134,44,0.12)` | `var(--warning)` |
| 风险/负面 | `rgba(206,93,87,0.10)` | `var(--danger)` |
| 灰色/未开始 | `rgba(122,145,184,0.12)` | `#68778f` |

---

### 5.11 列表表格

```css
/* 表头 */
th {
  padding: 14px 20px;
  font-size: 12px;
  font-weight: 800;
  color: #68778f;
  border-bottom: 1px solid rgba(122,145,184,0.10);
  background: rgba(248,251,255,0.80);
}

/* 单元格 */
td {
  padding: 16px 20px;
  font-size: 13px;
  border-bottom: 1px solid rgba(122,145,184,0.07);
}

/* 行悬停 */
tr:hover td {
  background: rgba(47,107,255,0.02);
}
```

---

### 5.12 进度条

```css
/* 轨道 */
.progress-track {
  height: 6px;
  background: rgba(122,145,184,0.12);
  border-radius: 999px;
  overflow: hidden;
}

/* 填充 */
.progress-bar {
  height: 100%;
  border-radius: 999px;
  background: var(--primary);  /* 或对应语义色 */
}
```

---

### 5.13 时间轴 Timeline

```
圆点: 14×14px, border:2px solid currentColor, background:#fff
连线: width:2px, background:rgba(122,145,184,0.2)
时间: 11px, 700, #9aa8bc
标题: 13px, 800, #1a2234
正文: 12px, 400, #68778f, line-height:1.6
```

---

### 5.14 SVG 图表

SVG 常用 `viewBox="0 0 360 140"` + `width="100%"`，实际渲染宽度会放大约 1.5–1.7 倍。

| 元素 | SVG 单位值 | 视觉约等于 |
|---|---|---|
| X 轴标签 font-size | `8` | ~13px |
| Y 轴标签 font-size | `8` | ~13px |
| 数据线 stroke-width | `2–2.5` | ~3–4px |
| 网格线 stroke-width | `1` | ~1.5px |

轴线色：`#e8edf5`；网格虚线：`#f0f3f8, stroke-dasharray:4,3`；标签色：`#9aa8bc`

---

## 六、间距速查

| 场景 | 值 |
|---|---|
| 顶栏高度 | 44px |
| 品牌栏高度 | 44px |
| 页面内容 padding | 14px 16px 32px |
| 卡片之间 gap | 20px |
| 卡片内边距（常规） | 16px 20px |
| 卡片内边距（宽松） | 18px 22px |
| 子块之间 gap | 12px |
| 列表行内边距 | 16px 20px |
| 区块标题 margin-bottom | 10–12px |

---

## 七、禁止事项

| ❌ 禁止 | ✅ 替代 |
|---|---|
| 卡片 `border-radius: 16px / 20px` | 统一 `10px` |
| 按钮 `border-radius: 12px` | 统一 `8px` |
| `background: var(--accent-gradient)` | 使用 `var(--primary)` 或具体渐变值 |
| `box-shadow: 0 8px 24px …`（重投影） | `0 1px 4px rgba(26,44,82,0.04)` |
| 毛玻璃 `backdrop-filter` | 不使用 |
| 浮动卡片 `position:fixed` 毛玻璃侧边栏 | 使用 sticky 贴边白色侧边栏 |
| SVG 标签 `font-size="13"` （viewBox坐标系） | 使用 `font-size="8"` |
